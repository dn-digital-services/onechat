rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

    // Every object lives under users/{uid}/... and is only ever
    // readable/writable by that same authenticated user.
    match /users/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

  }
}
